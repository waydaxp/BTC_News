pandas==2.2.2          # 最后兼容 NumPy 1.x 的版本
numpy==1.26.4          # TA-Lib Python 封装可编译通过的 1.x 最新版

yfinance>=0.2.30       # 拉取行情
requests>=2.31.0
beautifulsoup4>=4.13.4
lxml>=4.9.3            # 与 BS4 搭配解析网页
python-dateutil>=2.9.0
pytz                   # Binance SDK 仍依赖

matplotlib>=3.10.0     # 可选图表
seaborn>=0.13.2        # 主题配色
Pillow>=10.0.0         # 图像保存

python-binance>=1.0.17 # 币安多空持仓比
pybit>=5.3.0           # Bybit 数据

jinja2>=3.1            # HTML 模板渲染

ta-lib==0.4.26         # Python 封装，依赖 CI 编译安装的 TA-Lib C 库
